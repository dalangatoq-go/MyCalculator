# Copy this file to functions/.env (not committed to git) and fill in real values
# before deploying: firebase deploy --only functions

# OneSignal App ID (Settings > Keys & IDs in the OneSignal dashboard)
ONESIGNAL_APP_ID=

# OneSignal REST API Key (Settings > Keys & IDs) — keep this secret
ONESIGNAL_REST_API_KEY=
