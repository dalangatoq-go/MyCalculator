{
  "rules": {
    "presence": {
      "$alias": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.isBoolean() && newData.val() === true"
      }
    },
    "typing": {
      "$roomId": {
        "$alias": {
          ".read": "auth != null",
          ".write": "auth != null",
          ".validate": "newData.isBoolean() && newData.val() === true"
        }
      }
    },
    "deleted_for_me": {
      "$alias": {
        ".read": "auth != null",
        ".write": "auth != null",
        "$messageId": {
          ".validate": "newData.isBoolean() && newData.val() === true"
        }
      }
    }
  }
}
