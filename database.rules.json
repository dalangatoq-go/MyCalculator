{
  "rules": {
    "presence": {
      "$alias": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.isBoolean() && newData.val() === true"
      }
    },
    "typing": {
      "$roomId": {
        "$alias": {
          ".read": "auth != null",
          ".write": "auth != null",
          ".validate": "newData.isBoolean() && newData.val() === true"
        }
      }
    }
  }
}
